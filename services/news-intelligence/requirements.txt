fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0
sqlalchemy==2.0.32
psycopg2-binary==2.9.9
structlog==24.4.0
httpx==0.27.2
python-jose[cryptography]==3.3.0
redis>=5.0
apscheduler==3.10.4
feedparser==6.0.11
websockets==13.1
# T233-ARCH-INDICATOR-DEDUP: shared/common/__init__.py unconditionally imports indicators.py,
# which requires numpy/pandas — every service importing anything from common needs these two,
# even ones (like this one) that never call an indicator function directly. See CLAUDE.md's
# "api-gateway Crash-Loops on ModuleNotFoundError: No module named 'numpy'" entry for the exact
# same bug class this pin avoids.
numpy==1.26.4
pandas>=2.0.0
