# Audit Findings — 2026-06-09

Generated by: Claude Code
Scope: Second batch of improvements (JWT blacklist, global search, confidence trend, audit framework, live vs backtest panel, portfolio sector concentration)
Checklist used: `docs/AUDIT_CHECKLIST.md`

---

## P0 — Deploy Blockers

None.

---

## P1 — Fix Next Session

| # | File | Line | Finding | Fix |
|---|------|------|---------|-----|
| 1 | `shared/common/config.py` | 24 | JWT secret has a weak hardcoded default `"stockai-change-me-in-production-secret-key"`. Not random — a known literal. Production relies on the `JWT_SECRET` env var being set correctly. | Add startup validation: fail fast if `jwt_secret == "stockai-change-me-in-production-secret-key"` in non-dev environments, or log a loud warning. |
| 2 | `services/market-data/src/api/auth.py` | ~42 | Redis blacklist `_is_blacklisted()` returns `False` (fail-open) on exception — revoked tokens pass through if Redis is down. | Accepted trade-off for now (availability > security on Redis outage). Mitigated by short 7-day JWT expiry. Document this. |
| 3 | `services/api-gateway/src/api/proxy.py` | ~60 | Same Redis fail-open pattern in gateway blacklist check. | Same as above. |

---

## P2 — Backlog

| # | File | Line | Finding | Fix |
|---|------|------|---------|-----|
| 1 | `docker/docker-compose.yml` | 29 | Redis service was missing `restart: unless-stopped`. **FIXED in this session.** | Done ✓ |
| 2 | `services/market-data/src/services/scheduler.py` | ~875 | No rate-limiting between email sends. Multiple rapid signal transitions could burst-send. | Add per-alert cooldown (e.g., minimum 1 hour between sends on the same alert). Backlog. |
| 3 | `frontend/src/lib/auth.ts` | 39 | Logout fires server-side revocation fire-and-forget; if it fails, the token remains valid server-side until expiry (7 days). | Acceptable for most threat models. JTI expires in 7 days anyway. Low priority. |
| 4 | `services/signal-engine/src/api/routes.py` | 26–33 | Bare `except Exception: pass/return` in Redis cache helpers swallows errors silently. | Add `log.debug()` on exception so cache misses are visible in debug mode. |

---

## False Positives (investigated, not real issues)

- **Admin in _PUBLIC_PREFIXES**: `admin` is a routing key in the proxy route dict, NOT in `_PUBLIC_PREFIXES`. Admin routes require auth. Not a finding.
- **Signal history path ordering**: `/{symbol}/history` is at line 1776, `/{symbol}` at line 1821. History comes first — correct order. Not a finding.
- **React hook ordering**: All `useSWR` hooks in `signal-accuracy.tsx` are called before the `if (!authed) return null` early return. Not a finding.
- **Scheduler lambda crash risk**: APScheduler catches exceptions thrown by job functions — internal try/except + APScheduler safety net. Not a critical gap.

---

## Dimensions Covered

- [x] Dimension 1 — Auth & Security
- [ ] Dimension 2 — Data Integrity & DB (skipped — no DB schema changes this batch)
- [x] Dimension 3 — API Contract & Error Handling (partial)
- [x] Dimension 4 — Frontend State & UX
- [x] Dimension 5 — Scheduler & Background Jobs
- [ ] Dimension 6 — ML & Signal Engine (skipped — no ML changes this batch)
- [x] Dimension 7 — Docker & Deployment

## Dimensions Skipped

- Dim 2: No schema migrations in this batch; upsert/timestamp audit deferred.
- Dim 6: No ML model changes; signal history endpoint verified via path ordering check above.

---

## Notes

- All P1 items are accepted trade-offs (Redis fail-open) or minor hardening improvements — none are deploy blockers.
- The JWT secret weakness relies on `.env.production` having a strong `JWT_SECRET`. Confirmed it does. config.py default is never used in prod.
- Redis `restart: unless-stopped` was missing and has been fixed.
