# Audit Findings — 2026-06-10

Generated by: Claude Code (7-dimension workflow, 8 agents)
Scope: Full codebase — all 7 dimensions covered (Dim 2 & 6 first time; new ML/signal/login changes since 2026-06-09)
Checklist used: `docs/AUDIT_CHECKLIST.md`

---

## P0 — Deploy Blockers

None.

---

## P1 — All Fixed (commit `2453a40`, deployed `e96d791`)

| # | Dim | File | Line | Finding | Status |
|---|-----|------|------|---------|--------|
| 1 | Security | `shared/db/session.py` | 240 | Admin password hardcoded as plaintext `b"120402"` in `_seed_admin()` — committed to source control and baked into the image. | ✓ Reads from `ADMIN_PASSWORD` env var; skips seeding if empty |
| 2 | Security | `shared/common/service.py` | 34 | `allow_origins=['*']` unconditionally on all services including api-gateway. | ✓ Reads from `CORS_ORIGINS` env var; defaults to `*` in dev |
| 3 | Data Integrity | `shared/db/session.py` | 151–168 | `signal_alerts.alert_mode` column missing from `_run_migrations()` — existing prod DBs crash on queries referencing this column. | ✓ `ALTER TABLE signal_alerts ADD COLUMN IF NOT EXISTS alert_mode` added |
| 4 | Data Integrity | `shared/db/models.py` | 285 | `WatchlistItem` ORM declares `UniqueConstraint("user_id", "stock_id")` that was already dropped in migration — false alembic drift. | ✓ Removed stale constraint from ORM model |
| 5 | Routing | `services/signal-engine/src/api/routes.py` | 2144 | `GET /outcomes/summary` defined after `GET /{symbol}` — FastAPI routes it to `/{symbol}` with symbol='outcomes'. | ✓ Moved `/outcomes/summary` before `/{symbol}` |
| 6 | DB Query | `services/market-data/src/api/routes.py` | 1194–1255 | Short squeeze fetches ALL ranking rows for 7 days with no LIMIT — unbounded as table grows. | ✓ `ORDER BY stock_id, as_of ASC` + dict dedup keeps only latest per stock |
| 7 | API Contract | `services/market-data/src/api/routes.py` | 1452–1453 | `GET /stocks/{symbol}/dividends` returns `200 OK` with `"error"` key on upstream failure. | ✓ Raises `HTTPException(502)` |
| 8 | API Contract | `services/market-data/src/api/routes.py` | 1518–1520 | Same as #7 for `GET /stocks/{symbol}/institutional`. | ✓ Raises `HTTPException(502)` |
| 9 | Frontend UX | `frontend/src/pages/opportunities.tsx` | 875 | Earnings This Week panel silently absent during loading or on error. | ✓ `isLoading` + `error` destructured; spinner and error states rendered |
| 10 | Docker | `docker/docker-compose.yml` | 102–110 | `api-gateway depends_on` plain list loses `service_healthy` condition on cold boot. | ✓ All 10 `depends_on` entries use `condition: service_healthy` |
| 11 | Docker | `docker/docker-compose.yml` | 28–32 | Redis has no healthcheck — cannot upgrade to `service_healthy`. | ✓ Redis healthcheck added (`redis-cli ping`, 5s interval) |
| 12 | Docker | `docker/docker-compose.yml` | 34–47 | api-gateway can proxy to backend services before uvicorn is listening. | ✓ All 8 backend microservices use `condition: service_healthy` in api-gateway `depends_on` |
| 13 | ML | `services/signal-engine/src/api/routes.py` | 1633–1637 | `calibrate_ta_weights` reports in-sample accuracy — trivially overfits. | ✓ Uses `cross_val_score(TimeSeriesSplit(n_splits=5))`; fits on full data for weights |
| 14 | ML | `services/signal-engine/src/api/routes.py` | 614–733 | `calibrate_ml_weight` selects weight on full history — equivalent to test-set hyperparameter tuning. | ✓ 70/30 temporal train/val split; weight selected on older 70%, accuracy on newer 30% |
| 15 | ML | `services/ml-prediction/src/training/tuner.py` | 130 | Optuna study has no sampler seed — non-reproducible tuning runs. | ✓ `sampler=TPESampler(seed=42)` added |
| 16 | Scheduler | `services/market-data/src/services/scheduler.py` | 1132–1133 | `_symbols_for` calls outside `try` block — DB failure bypasses `_record_job_status`. | ✓ Moved inside `try` block |
| 17 | Signal Logic | `services/signal-engine/src/api/routes.py` | 1342–1362 | `days_active` streak never updates `prev_ts` — streak inflated across outages/holidays. | ✓ `prev_ts = ts` assigned in loop; gap-break resets active on >2-day gaps |

---

## P2 — Backlog

| # | Dim | File | Line | Finding | Fix |
|---|-----|------|------|---------|-----|
| 1 | Security | `services/api-gateway/src/api/proxy.py` | 116 | Internal `httpx` exception details forwarded verbatim (includes internal service URLs like `http://market-data:8001/...`). | Log server-side; return `"Upstream service unavailable"` to client. |
| 2 | Auth | `frontend/src/lib/auth.ts` | 30–32 | Token written to `localStorage` before `decodeJWT` check. If token arrives expired, invalid token is left in storage (though `getSession()` correctly returns null — no access granted). | Move `localStorage.setItem` after `decodeJWT` validation; or add `localStorage.removeItem` before `return false`. |
| 3 | Performance | `services/market-data/src/api/routes.py` | 1528–1529 | `r.keys('conv_gate:*')` is a blocking O(N) Redis KEYS scan. Blocks Redis server while iterating all keys. | Replace with `r.scan_iter('conv_gate:*')` (non-blocking batches, already used at line 151 for `signals:cache:*`). |
| 4 | Performance | `services/market-data/src/api/routes.py` | 1052, 1116, 1159, 1202 | Four endpoints (`sector_performance`, `earnings_calendar`, `analyst_ratings`, `short_squeeze`) do `select(Stock)` fetching all 11 columns when only 4–5 are used. | Use explicit column projections: `select(Stock.id, Stock.symbol, Stock.name, Stock.sector, Stock.market)`. |
| 5 | Performance | `services/api-gateway/src/api/proxy.py` | 105 | Single 120s httpx timeout for ALL upstream requests. A slow backend holds an async worker for 2 minutes, exhausting worker pool under concurrency. | Per-route timeouts: 15s for data routes, keep 120s for AI/ML routes. |
| 6 | Performance | `services/market-data/src/api/routes.py` | 1302–1410 | `GET /stocks/{symbol}/options-flow` has no timeout on yfinance blocking calls. | Wrap yfinance calls in `ThreadPoolExecutor` with a future timeout (pattern already used in `_fetch_live_bulk`). |
| 7 | Performance | `services/market-data/src/api/routes.py` | 734–952 | `GET /stocks/{symbol}/fundamentals` makes multiple sequential blocking yfinance calls with no timeouts. | Set timeout on the yfinance session or enforce wall-clock limit via `ThreadPoolExecutor` timeout. |
| 8 | Performance | `services/signal-engine/src/api/routes.py` | 2025–2027 | `evaluate_signal_outcomes` and `outcomes_summary` load ALL matching rows with no LIMIT. Grows unbounded over time. | Use date-windowed batches or add a hard cap on the `days` parameter. |
| 9 | Data Integrity | `shared/db/models.py` | 160 | `Signal.ts` uses `default=func.now()` (Python-side) not `server_default=func.now()`. Direct pg_insert calls bypass Python defaults, risking NULL on a NOT NULL column. | Change to `server_default=func.now()` to match all other timestamp columns. |
| 10 | Data Integrity | `shared/db/models.py` | 285 | See P1 #4 — also produces false drift in alembic autogenerate. Backlog for schema cleanup. | Already listed as P1; secondary concern is alembic hygiene. |
| 11 | Observability | `services/market-data/src/api/routes.py` | 209–224 | Bare `except: pass` in `_fetch_live_bulk` silently drops per-symbol failures with no log trace. | Add `log.debug(...)` in each bare except. |
| 12 | Observability | `services/signal-engine/src/api/routes.py` | 175–206 | `_bulk_persist` background task: per-symbol try/except exists but no top-level handler. Infrastructure failure outside the symbol loop terminates the task silently. | Wrap entire `_bulk_persist` body in top-level try/except with `log.error`. |
| 13 | Observability | `services/signal-engine/src/api/routes.py` | 1237–1238 | Internal HTTP call to market-data in `/signals/suppressed` silently swallowed on exception — conviction column silently empty if market-data is down. | Add `log.warning("conviction.fetch_failed", error=str(exc))`. |
| 14 | Observability | `services/market-data/src/services/scheduler.py` | 664–912 | Alert checkers have no explicit `session.rollback()` on failure paths. SQLAlchemy pool cleanup handles it in practice but relies on pool-level hygiene. | Add explicit `try: session.rollback() except: pass` in each alert checker exception handler. |
| 15 | ML Design | `services/signal-engine/src/generators/signals.py` | 1277–1288 | Short-interest squeeze boost is asymmetric — inflates BUY signals only (fires when `fused > 0.5`), no corresponding dampening for SELL direction. A 0.02 probability difference produces a regime change at the boundary. | Document intent explicitly. Consider symmetric bearish dampening. |
| 16 | ML Data | `services/ml-prediction/src/training/trainer.py` | 439 | Missing feature columns filled with `0.0` — a real value in most feature distributions. If macro features fail to download (yfinance outage), 8 macro columns become `0.0`, biasing predictions without warning. | Log warning when any feature column is all-NaN before fillna. Use median training values for continuous features; reserve `0.0` for boolean flags. |
| 17 | Docker | `docker/docker-compose.yml` | 1 | No `docker-compose.prod.yml` override. One compose file for dev and prod — no place to enforce image digest pinning, resource limits, or stripped debug ports. | Create `docker/docker-compose.prod.yml` override for prod-only settings. |
| 18 | Docker | `services/api-gateway/Dockerfile` | 1 | All Python images use `python:3.11-slim` (floating patch), Node uses `node:20-alpine` (floating patch). Registry push of a patched image can silently change prod behaviour. | Pin each base image to a full SHA digest in production. |
| 19 | Docker | `frontend/Dockerfile` | 10–11 | `API_GATEWAY_URL` ARG has a default value — building without passing the ARG silently uses the default with no assertion failure. | Remove ARG default; add `RUN test -n "$API_GATEWAY_URL" || exit 1` guard before `npm run build`. |
| 20 | Frontend UX | `frontend/src/pages/opportunities.tsx` | 967 | `localStorage.setItem` in strategy selector `onClick` has no `typeof window !== 'undefined'` guard (Pages Router: SSR can still run). | Wrap with `if (typeof window !== 'undefined')`. |
| 21 | Frontend UX | `frontend/src/pages/stock/[symbol].tsx` | 143 | `activeNewsSources()` called at component top level — SWR key can differ between SSR and hydration, triggering extra fetch and flash. | Move into `useMemo` with SSR-safe fallback. |
| 22 | Frontend UX | `frontend/src/pages/opportunities.tsx` | 903–958 | Earnings This Week cards use `<a href={...}>` (full page reload) instead of Next.js `<Link>`. Loses all SWR cache on every click. | Replace with `<Link>` (already imported). |
| 23 | Frontend Keys | `frontend/src/pages/opportunities.tsx` | 823, 1117–1119 | Array index `key={i}` on catalyst list items and opportunity reason bullets. | Key by content: `key={c}` / `key={rr.text}`. |
| 24 | Frontend Keys | `frontend/src/pages/stock/[symbol].tsx` | 1233, 1264, 1569, 1604 | Array index keys on S/R levels, chart patterns, game plan entries, and game plan catalysts. | Key by stable content: `key={\`${lvl.kind}-${lvl.price}\`}`, `key={p.name}`, `key={e.label}`, `key={c}`. |
| 25 | Documentation | `services/market-data/src/adapters/base.py` | 57–68 | Docstring says "16:00 UTC" for HK daily bar storage; actual format is midnight UTC-naive (matching local trading date). A developer correcting code to match the comment would break date recovery. | Update docstring to accurately describe midnight UTC-naive storage. |

---

## Previously Known / Accepted (not re-escalated)

- **JWT weak default** (`shared/common/config.py` line 24) — prod uses env var; startup guard exists. No regression.
- **Redis blacklist fails-open** (`jwt_auth.py`, `proxy.py`) — accepted availability trade-off; 7-day JWT expiry mitigates. No regression.
- **No email alert rate-limiting** (`scheduler.py`) — P2 backlog, unchanged.
- **auth.ts logout fire-and-forget** — low priority; token expires in 7 days regardless.

---

## False Positives Investigated

- JWT weak default guard present in config.py (model_post_init raises in prod) — not a regression
- Redis blacklist fails-open — accepted trade-off, unchanged
- Ingest upsert: `pg_insert` with `on_conflict_do_update` used correctly — no blind inserts
- SQL injection in session.py: all `text()` calls use named bound parameters — clean
- Auth token attachment in api.ts: all requests through `request()` with `authHeader()` — clean
- Hardcoded API URLs: none found; all paths use `BASE` constant defaulting to `/api`
- localStorage in alerts.tsx: all reads inside `useEffect` (client-only) — safe
- SWR key collisions in opportunities.tsx: all keys unique; shared keys intentional for cache reuse
- React hook order in stock/[symbol].tsx: all hooks before any early returns — clean
- `GET /outcomes/evaluate` (POST) routing: POST doesn't conflict with `GET /{symbol}` — safe
- `calibrate_ml_weight` weight injection: weights swept from hardcoded `[0.00..1.00]` list — no injection risk
- Signal history endpoint ordering: `ts.asc()` confirmed — correct
- ML training CV metrics in trainer.py: `TimeSeriesSplit(n_splits=5)` with OOS fold — genuine CV
- Feature list consistency: artifact bundle saves/restores `feature_columns` — no train/serve skew
- Short interest boost overflow: `np.clip(..., 0.0, 1.0)` applied — confirmed cannot overflow

---

## Dimensions Covered

- [x] Dimension 1 — Auth & Security
- [x] Dimension 2 — Data Integrity & DB *(first full coverage)*
- [x] Dimension 3 — API Contract & Error Handling
- [x] Dimension 4 — Frontend State & UX
- [x] Dimension 5 — Scheduler & Background Jobs
- [x] Dimension 6 — ML & Signal Engine *(first full coverage)*
- [x] Dimension 7 — Docker & Deployment
